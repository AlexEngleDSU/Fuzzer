name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Set up Go
        uses: actions/setup-go@v5
        with: { go-version-file: 'go.mod' }

      - name: Build Binaries
        run: |
          mkdir -p bin
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o bin/fuzzer-windows.exe .
          GOOS=linux   GOARCH=amd64 go build -ldflags="-s -w" -o bin/fuzzer-linux .
          GOOS=darwin  GOARCH=amd64 go build -ldflags="-s -w" -o bin/fuzzer-darwin .

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
